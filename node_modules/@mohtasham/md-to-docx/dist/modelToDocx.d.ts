import { Paragraph, Table } from "docx";
import type { DocxDocumentModel } from "./docxModel.js";
import { Style, Options } from "./types.js";
/**
 * Converts internal docx model to docx Paragraph/Table objects
 * Handles nested lists with proper level tracking
 */
export declare function modelToDocx(model: DocxDocumentModel, style: Style, options: Options): Promise<{
    children: (Paragraph | Table)[];
    headings: {
        text: string;
        level: number;
        bookmarkId: string;
    }[];
    maxSequenceId: number;
}>;
